// window.onload = () => {
//   let divs = document.querySelectorAll("div");
//   divs.forEach((div) => {
//     // avoid overwriting the innerHTML of any nested divs
//     if (div.children.length > 0) return;
//     div.innerHTML = div.className;
//   });
// };
